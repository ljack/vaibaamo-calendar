## Summary
This PR implements Passkey (WebAuthn) authentication support using custom Supabase Edge Functions and a dedicated database schema.

## Key Changes
1. **Migrations**: Added `passkeys` and `webauthn_challenges` tables.
2. **Edge Function**: Custom WebAuthn flow in `supabase/functions/auth-webauthn`.
3. **Frontend**: added `passkeyService.ts`, updated `AuthContext` and `Login.tsx`.
4. **Onboarding**: Added `PasskeyPrompt` for post-login registration.

## Verification
- Build passed successfully.
- Code push complete.
- Manual verification instructions included in walkthrough.md.
